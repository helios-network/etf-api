# Development overrides for docker-compose.yml
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  redis:
    # Expose Redis port for local development/debugging
    ports:
      - "6379:6379"
